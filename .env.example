# ==============================================================================
# OpenAI API Configuration
# ==============================================================================
# Required: Your OpenAI API key
# Format: sk-xxxxxx
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI API base URL
# Default: https://api.openai.com/v1
# Use this to configure custom endpoints or proxy
OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: OpenAI model name
# Default: gpt-4
# Examples: gpt-4, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4

# ==============================================================================
# Embedding Configuration
# ==============================================================================
# Optional: Embedding API base URL
# Default: Uses OPENAI_BASE_URL if not specified
# Use this to configure a different endpoint for embeddings
EMBEDDING_BASE_URL=https://api.openai.com/v1

# Optional: Embedding model name
# Default: text-embedding-3-small
# Examples: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
EMBEDDING_MODEL=text-embedding-3-small

# ==============================================================================
# Model Parameters
# ==============================================================================
# Optional: Model temperature (0-1)
# Default: 0
# Lower values make output more deterministic
MODEL_TEMPERATURE=0

# Optional: Model top_p (0-1)
# Default: 1
MODEL_TOP_P=1

# ==============================================================================
# Cache and State Configuration
# ==============================================================================
# Optional: Cache directory path
# Default: .cache
CACHE_DIR=.cache

# Optional: State directory path
# Default: .state
STATE_DIR=.state

# ==============================================================================
# Logging Configuration
# ==============================================================================
# Optional: Log level
# Default: info
# Options: error, warn, info, debug, verbose, silly
LOG_LEVEL=info

# Optional: Enable file logging
# Default: false (enabled in development mode)
# Set to 'true' to enable file logging to logs/ directory
ENABLE_FILE_LOG=false

# Optional: Enable console logging
# Default: false (enabled in development mode)
# Set to 'true' to enable console logging (may interfere with stdio transport)
ENABLE_CONSOLE_LOG=false

# Optional: Enable metrics logging
# Default: false
# Set to 'true' to enable detailed metrics logging
LOG_METRICS=false

# Optional: Node environment
# Default: production
# Options: development, production, test
NODE_ENV=production

# ==============================================================================
# Transport Configuration (HTTP Streaming Mode)
# ==============================================================================
# Optional: HTTP server port
# Default: 3000
HTTP_PORT=3000

# Optional: HTTP server host
# Default: localhost
# Use 0.0.0.0 to listen on all interfaces
HTTP_HOST=localhost

# Optional: HTTP endpoint path
# Default: /mcp
# The MCP endpoint path (must start with /)
HTTP_ENDPOINT=/mcp

# ==============================================================================
# Worker Configuration
# ==============================================================================
# Optional: Enable worker pool
# Default: true
# Set to 'false' to disable worker-based execution
WORKER_ENABLED=true

# Optional: Maximum worker pool size
# Default: 3
# Controls concurrent worker threads
WORKER_MAX_POOL=3

# ==============================================================================
# Workspace Configuration
# ==============================================================================
# Optional: Workspace cleanup interval in milliseconds
# Default: 600000 (10 minutes)
WORKSPACE_CLEANUP_INTERVAL=600000

# Optional: Workspace maximum age in milliseconds
# Default: 3600000 (1 hour)
WORKSPACE_MAX_AGE=3600000

# ==============================================================================
# Test Fix Configuration
# ==============================================================================
# Optional: Maximum fix attempts for failing tests
# Default: 3
FIX_MAX_ATTEMPTS=3

# Optional: Confidence threshold for auto-fix (0-1)
# Default: 0.7
# Higher values require more confidence before applying fixes
FIX_CONFIDENCE_THRESHOLD=0.7

# ==============================================================================
# Test Generation Configuration
# ==============================================================================
# Optional: Maximum number of tests to generate per file
# Default: 10
MAX_TESTS=10

# ==============================================================================
# Tracking and Monitoring Configuration
# ==============================================================================
# Optional: Enable tracking service
# Default: false
# Set to 'true' to enable metrics tracking and monitoring
TRACKING_ENABLED=false

# Optional: Tracking application ID
# Default: MCP_SERVICE
# Used for identifying the application in tracking system
TRACKING_APP_ID=MCP_SERVICE

# Optional: Tracking application version
# Default: (none)
# Version identifier for tracking
TRACKING_APP_VERSION=3.0.0

# Optional: Tracking environment
# Default: prod
# Options: dev (no tracking), test, prod
TRACKING_ENV=prod

# Optional: Tracking measurement name
# Default: mcp_service_metrics
TRACKING_MEASUREMENT=mcp_service_metrics

# Optional: Tracking metrics type
# Default: metricsType1
TRACKING_METRICS_TYPE=metricsType1

# ==============================================================================
# Advanced Configuration
# ==============================================================================
# Optional: Custom config file path
# Default: config.yaml in project root
# Uncomment to use a custom configuration file
# CONFIG_PATH=/path/to/custom/config.yaml

# Optional: Project root directory
# Default: Auto-detected from diff
# Uncomment to manually specify project root
# PROJECT_ROOT=/path/to/your/project

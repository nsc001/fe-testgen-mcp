# ==============================================================================
# OpenAI API 配置
# ==============================================================================
# 必填：OpenAI API 密钥
# 格式：sk-xxxxxx
OPENAI_API_KEY=your_openai_api_key_here

# 可选：OpenAI API 基础 URL
# 默认值：https://api.openai.com/v1
# 用于配置自定义端点或代理
OPENAI_BASE_URL=https://api.openai.com/v1

# 可选：OpenAI 模型名称
# 默认值：gpt-4
# 示例：gpt-4, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4

# ==============================================================================
# Embedding 配置
# ==============================================================================
# 可选：Embedding API 基础 URL
# 默认值：如果未指定则使用 OPENAI_BASE_URL
# 用于配置不同的 embedding 端点
EMBEDDING_BASE_URL=https://api.openai.com/v1

# 可选：Embedding 模型名称
# 默认值：text-embedding-3-small
# 示例：text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
EMBEDDING_MODEL=text-embedding-3-small

# ==============================================================================
# 模型参数
# ==============================================================================
# 可选：模型温度参数（0-1）
# 默认值：0
# 较低的值使输出更确定性
MODEL_TEMPERATURE=0

# 可选：模型 top_p 参数（0-1）
# 默认值：1
MODEL_TOP_P=1

# ==============================================================================
# 缓存和状态配置
# ==============================================================================
# 可选：缓存目录路径
# 默认值：.cache
CACHE_DIR=.cache

# 可选：状态目录路径
# 默认值：.state
STATE_DIR=.state

# ==============================================================================
# 日志配置
# ==============================================================================
# 可选：日志级别
# 默认值：info
# 可选项：error, warn, info, debug, verbose, silly
LOG_LEVEL=info

# 可选：启用文件日志
# 默认值：false（开发模式下自动启用）
# 设置为 'true' 以启用文件日志到 logs/ 目录
ENABLE_FILE_LOG=false

# 可选：启用控制台日志
# 默认值：false（开发模式下自动启用）
# 设置为 'true' 以启用控制台日志（可能会干扰 stdio 传输模式）
ENABLE_CONSOLE_LOG=false

# 可选：启用指标日志
# 默认值：false
# 设置为 'true' 以启用详细的指标日志
LOG_METRICS=false

# 可选：Node 环境
# 默认值：production
# 可选项：development, production, test
NODE_ENV=production

# ==============================================================================
# 传输配置（HTTP Streaming 模式）
# ==============================================================================
# 可选：HTTP 服务器端口
# 默认值：3000
HTTP_PORT=3000

# 可选：HTTP 服务器主机
# 默认值：localhost
# 使用 0.0.0.0 监听所有网络接口
HTTP_HOST=localhost

# 可选：HTTP 端点路径
# 默认值：/mcp
# MCP 端点路径（必须以 / 开头）
HTTP_ENDPOINT=/mcp

# ==============================================================================
# Worker 配置
# ==============================================================================
# 可选：启用 worker 池
# 默认值：true
# 设置为 'false' 以禁用基于 worker 的执行
WORKER_ENABLED=true

# 可选：最大 worker 池大小
# 默认值：3
# 控制并发 worker 线程数
WORKER_MAX_POOL=3

# ==============================================================================
# 工作区配置
# ==============================================================================
# 可选：工作区清理间隔（毫秒）
# 默认值：600000（10 分钟）
WORKSPACE_CLEANUP_INTERVAL=600000

# 可选：工作区最大存活时间（毫秒）
# 默认值：3600000（1 小时）
WORKSPACE_MAX_AGE=3600000

# ==============================================================================
# 测试修复配置
# ==============================================================================
# 可选：失败测试的最大修复尝试次数
# 默认值：3
FIX_MAX_ATTEMPTS=3

# 可选：自动修复的置信度阈值（0-1）
# 默认值：0.7
# 较高的值需要更高的置信度才会应用修复
FIX_CONFIDENCE_THRESHOLD=0.7

# ==============================================================================
# 测试生成配置
# ==============================================================================
# 可选：每个文件最多生成的测试数量
# 默认值：10
MAX_TESTS=10

# ==============================================================================
# 监控和追踪配置
# ==============================================================================
# 可选：启用追踪服务
# 默认值：false
# 设置为 'true' 以启用指标追踪和监控
TRACKING_ENABLED=false

# 可选：追踪应用 ID
# 默认值：MCP_SERVICE
# 用于在追踪系统中标识应用
TRACKING_APP_ID=MCP_SERVICE

# 可选：追踪应用版本
# 默认值：（无）
# 追踪的版本标识符
TRACKING_APP_VERSION=3.0.0

# 可选：追踪环境
# 默认值：prod
# 可选项：dev（不追踪）、test、prod
TRACKING_ENV=prod

# 可选：追踪测量名称
# 默认值：mcp_service_metrics
TRACKING_MEASUREMENT=mcp_service_metrics

# 可选：追踪指标类型
# 默认值：metricsType1
TRACKING_METRICS_TYPE=metricsType1

# ==============================================================================
# 高级配置
# ==============================================================================
# 可选：自定义配置文件路径
# 默认值：项目根目录的 config.yaml
# 取消注释以使用自定义配置文件
# CONFIG_PATH=/path/to/custom/config.yaml

# 可选：项目根目录
# 默认值：从 diff 自动检测
# 取消注释以手动指定项目根目录
# PROJECT_ROOT=/path/to/your/project
